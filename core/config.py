"""
키워드 필터링 설정 및 점수 가중치 정의
"""

# ==============================================================
# 포함 키워드 — 적중 시 점수 상승
# ==============================================================
INCLUDE_KEYWORDS = [
    "자동화",
    "LLM",
    "RAG",
    "챗봇",
    "Agent",
    "에이전트",
    "생성형",
    "GPT",
    "AI",
    "머신러닝",
    "딥러닝",
    "NLP",
    "자연어",
]

# 키워드별 가중치 (기본값 = 1)
KEYWORD_WEIGHTS = {
    "Agent":   2,
    "에이전트": 2,
    "LLM":     2,
    "RAG":     2,
    "자동화":   1,
    "챗봇":    1,
    "생성형":   1,
    "GPT":     1,
    "AI":      1,
    "머신러닝":  1,
    "딥러닝":   1,
    "NLP":     1,
    "자연어":   1,
}

# 동시 적중 보너스 (여러 키워드가 함께 있을 때 추가 점수)
COMBO_BONUSES = [
    ({"LLM", "RAG"}, 3),
    ({"LLM", "Agent"}, 2),
    ({"LLM", "에이전트"}, 2),
]

# ==============================================================
# 제외 키워드 — 포함 시 즉시 필터링
# ==============================================================

# 단순 포함 매칭 (부분 문자열이 있으면 제외)
EXCLUDE_KEYWORDS_PARTIAL = [
    # 네트워크·보안
    "네트워크", "보안", "정보보호",
    # 하드웨어·시스템
    "임베디드", "펌웨어", "반도체", "하드웨어", "H/W", "인프라",
    # 개발 언어 (Python 제외)
    "Java", "Spring", ".NET", "C++", "C#",
    # 직군
    "백엔드", "프론트엔드", "웹개발", "앱개발", "풀스택", "윈도우개발",
    # 클라우드·인프라
    "DevOps", "AWS", "클라우드",
    # 데이터·AI 비관련
    "데이터사이언티스트", "MLOps", "비전", "이미지처리", "영상처리",
    # 기타
    "QA", "UX", "UI", "GUI", "DBA", " DA ", "로봇", "자율주행",
    "LiDAR", "알고리즘", "설계",
]

# 구문 매칭 — 정확히 해당 구문이 있을 때만 제외
EXCLUDE_KEYWORDS_PHRASE = [
    "시스템엔지니어",
    "시스템 엔지니어",
]

# 제목에서만 적용하는 C 언어 단독 제외 패턴 (정규식)
# "C언어", "C개발", " C " 등 — Python은 절대 제외 안 함
EXCLUDE_C_PATTERN = r"\bC\b(?!\+\+|#)"   # 단독 C (C++, C# 제외)

# 근무지 필터 (서울/경기 외 지역 제외)
ALLOWED_REGIONS = ["서울", "경기", "판교", "성남", "수원", "용인", "화성", "광명", "안양", "과천", "의왕", "군포", "시흥"]

# 마감일 필터 (오늘로부터 N일 이내)
DEADLINE_DAYS_LIMIT = 60

# ==============================================================
# 이메일 설정
# ==============================================================
RECIPIENT_NAME = "예슬"
MIN_SCORE_TO_SEND = 0   # 0점 이상이면 발송 (필터 통과한 전체)
MAX_JOBS_PER_EMAIL = 30  # 이메일에 담을 최대 공고 수
