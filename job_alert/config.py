"""
키워드 필터링 설정 및 점수 가중치 정의
환경 변수 우선 → 없으면 하드코딩 기본값 사용
"""
import os

# ==============================================================
# 이메일 설정
# ==============================================================
RECIPIENT_NAME: str = os.getenv("RECIPIENT_NAME", "예슬")
GMAIL_SENDER: str = os.getenv("GMAIL_SENDER", "")
GMAIL_APP_PASSWORD: str = os.getenv("GMAIL_APP_PASSWORD", "")
MAIL_TO: str = os.getenv("MAIL_TO", GMAIL_SENDER)

MAX_JOBS_PER_EMAIL: int = 30
MIN_SCORE_TO_SEND: int = 0   # 0 = 필터 통과 전체 발송

# ==============================================================
# 포함 키워드 (점수 상승)
# ==============================================================
INCLUDE_KEYWORDS: list[str] = [
    "자동화",
    "LLM",
    "RAG",
    "챗봇",
    "Agent",
    "에이전트",
    "생성형",
    "GPT",
    "AI",
    "머신러닝",
    "딥러닝",
    "NLP",
    "자연어",
]

KEYWORD_WEIGHTS: dict[str, int] = {
    "Agent":    2,
    "에이전트":  2,
    "LLM":      2,
    "RAG":      2,
    "자동화":    1,
    "챗봇":     1,
    "생성형":    1,
    "GPT":      1,
    "AI":       1,
    "머신러닝":  1,
    "딥러닝":   1,
    "NLP":      1,
    "자연어":   1,
}

# 동시 적중 보너스
COMBO_BONUSES: list[tuple[set, int]] = [
    ({"LLM", "RAG"}, 3),
    ({"LLM", "Agent"}, 2),
    ({"LLM", "에이전트"}, 2),
]

# ==============================================================
# 제외 키워드 (포함 시 즉시 제거)
# ==============================================================

# 부분 포함 매칭
EXCLUDE_KEYWORDS_PARTIAL: list[str] = [
    # 네트워크·보안
    "네트워크", "보안", "정보보호",
    # 하드웨어·시스템
    "임베디드", "펌웨어", "반도체", "하드웨어", "H/W", "인프라",
    # 개발 언어 (Python 제외)
    "Java", "Spring", ".NET", "C++", "C#",
    # 직군
    "백엔드", "프론트엔드", "웹개발", "앱개발", "풀스택", "윈도우개발",
    # 클라우드·인프라
    "DevOps", "AWS", "클라우드",
    # 데이터·AI 비관련
    "데이터사이언티스트", "MLOps", "비전", "이미지처리", "영상처리",
    # 기타
    "QA", "UX", "UI", "GUI", "DBA", "로봇", "자율주행", "LiDAR", "알고리즘", "설계",
]

# 구문 단위 매칭 (정확히 해당 구문일 때만 제외)
EXCLUDE_KEYWORDS_PHRASE: list[str] = [
    "시스템엔지니어",
    "시스템 엔지니어",
]

# 단독 C 언어 제외 정규식 (C++, C# 은 위에서 처리)
EXCLUDE_C_PATTERN: str = r"(?<![A-Za-z가-힣])C(?![A-Za-z+#])"

# 허용 근무지역
ALLOWED_REGIONS: list[str] = [
    "서울", "경기", "판교", "성남", "수원", "용인", "화성",
    "광명", "안양", "과천", "의왕", "군포", "시흥", "인천",
]

DEADLINE_DAYS_LIMIT: int = 60
